package I개입중단

import . "unsafe"
import . "reflect"

import . "입출력접속"
import . "전역서술자표"
import . "다중작업관리"
import . "단말"

func 개입중단무시()

func 개입중단예외처리기()
func 개입중단예외처리기0x00()
func 개입중단예외처리기0x01()
func 개입중단예외처리기0x02()
func 개입중단예외처리기0x03()
func 개입중단예외처리기0x04()
func 개입중단예외처리기0x05()
func 개입중단예외처리기0x06()
func 개입중단예외처리기0x07()
func 개입중단예외처리기0x08()
func 개입중단예외처리기0x09()
func 개입중단예외처리기0x0a()
func 개입중단예외처리기0x0b()
func 개입중단예외처리기0x0c()
func 개입중단예외처리기0x0d()
func 개입중단예외처리기0x0e()
func 개입중단예외처리기0x0f()
func 개입중단예외처리기0x10()
func 개입중단예외처리기0x11()
func 개입중단예외처리기0x12()
func 개입중단예외처리기0x13()

func 개입중단요청처리기0x00()
func 개입중단요청처리기0x01()
func 개입중단요청처리기0x02()
func 개입중단요청처리기0x03()
func 개입중단요청처리기0x04()
func 개입중단요청처리기0x05()
func 개입중단요청처리기0x06()
func 개입중단요청처리기0x07()
func 개입중단요청처리기0x08()
func 개입중단요청처리기0x09()
func 개입중단요청처리기0x0a()
func 개입중단요청처리기0x0b()
func 개입중단요청처리기0x0c()
func 개입중단요청처리기0x0d()
func 개입중단요청처리기0x0e()
func 개입중단요청처리기0x0f()

func 개입중단요청처리기0x80()
func 개입중단요청처리기0x81()
func 개입중단요청처리기0x82()

func T시험출력(위치 uint8, 자료 uint8)
func 자료구간선택자설정(ds구간 uint32)
func 추가구간선택자설정(gs구간 uint32)
func 개입중단종료대기()

type T개입중단처리기 struct {
	T개입중단번호		uint8
	I개입중단관리자	uintptr
}
type I개입중단처리기 interface {
	H처리개입중단(uint32) uint32
}

func F개입중단처리기생성(I개입중단관리자 uintptr, T개입중단번호 uint8) *T개입중단처리기 {
	개입중단처리기_2 := new(T개입중단처리기)
	개입중단처리기_2.T개입중단번호 = T개입중단번호
	개입중단처리기_2.I개입중단관리자 = I개입중단관리자
	return 개입중단처리기_2

}

var 처리기_2 [256]uintptr

func (자신 *T개입중단처리기) F관문초기화(T개입중단번호 uint8, I개입중단관리자 uintptr, 함수주소 uintptr) {

	처리기_2[T개입중단번호] = 함수주소

	자신.T개입중단번호 = T개입중단번호
	자신.I개입중단관리자 = I개입중단관리자

}
func (자신 *T개입중단처리기) M개입중단처리함수설정(T개입중단번호 uint32, 주소 uintptr) {
	처리기_2[T개입중단번호] = 주소
}
func (자신 *T개입중단처리기) M등록해제() {
	자신uintptr := uintptr(Pointer(자신))
	I개입중단관리자 := (*T개입중단관리자)(Pointer(자신.I개입중단관리자))
	if 자신uintptr == I개입중단관리자.M처리기가져오기(자신.T개입중단번호) {
		I개입중단관리자.M처리기설정(0, 자신.T개입중단번호)
	}

}
func (자신 *T개입중단처리기) M개입중단관리자설정(I개입중단관리자 uintptr) {
}
func (자신 *T개입중단처리기) M개입중단번호설정(T개입중단번호 uint8) {
	자신.T개입중단번호 = T개입중단번호
}
func (자신 *T개입중단처리기) H처리개입중단(확장쌓임공간주소 uint32) uint32 {
	임시공간 := []byte("\n\n\n\n\n   TInterruptHandler")
	단말_3 := T단말{}
	단말_3.M출력(임시공간)
	return 확장쌓임공간주소
}
func F개입중단진단출력() {
	임시공간 := []byte("\n\n\n\n   TInterruptHandler")
	단말_3 := T단말{}
	단말_3.M출력(임시공간)
}

type T관문서술자 struct {
	관문자료 [8]uint8
}
type T개입중단서술자표주소참조 struct {
}

var 개입중단서술자표자료 [256 * 8]uint8
var A활성개입중단관리자 uintptr = 0

const 개입중단추적 = false

type T개입중단관리자 struct {
	처리기_2	[256]uintptr

	물리장치개입중단변위	uint16

	작업관리자	*T작업관리자
}

var P주PIC명령입출력접속 uint16 = 0x20
var P주PIC자료입출력접속 uint16 = 0x21
var P보조PIC명령입출력접속 uint16 = 0xA0
var P보조PIC자료입출력접속 uint16 = 0xA1

func (자신 *T개입중단관리자) F관문초기화(물리장치개입중단변위 uint16, 전역서술자표_2 *T전역서술자표, 작업관리자 *T작업관리자) {

	자신.작업관리자 = 작업관리자

	자신.물리장치개입중단변위 = 물리장치개입중단변위
	코드구간 := uint16(Seg핵심코드)

	for i := 0; i < (256 * 8); i++ {
		개입중단서술자표자료[i] = 0
	}
	var 주소 uint32
	var I개입중단서술자표개입중단관문 uint8 = 0xE
	주소 = uint32(ValueOf(개입중단무시).Pointer())
	for i := 0; i < 256; i++ {
		처리기_2[i] = 0
		주소 = uint32(ValueOf(개입중단예외처리기0x0f).Pointer())
		자신.M개입중단서술자표항목설정(i, 코드구간, 주소, 0, I개입중단서술자표개입중단관문)
	}

	주소 = uint32(ValueOf(개입중단예외처리기0x00).Pointer())
	자신.M개입중단서술자표항목설정(0x00, 코드구간, 주소, 0, I개입중단서술자표개입중단관문)

	주소 = uint32(ValueOf(개입중단예외처리기0x01).Pointer())
	자신.M개입중단서술자표항목설정(0x01, 코드구간, 주소, 0, I개입중단서술자표개입중단관문)
	주소 = uint32(ValueOf(개입중단예외처리기0x02).Pointer())
	자신.M개입중단서술자표항목설정(0x02, 코드구간, 주소, 0, I개입중단서술자표개입중단관문)

	주소 = uint32(ValueOf(개입중단예외처리기0x03).Pointer())
	자신.M개입중단서술자표항목설정(0x03, 코드구간, 주소, 0, I개입중단서술자표개입중단관문)

	주소 = uint32(ValueOf(개입중단예외처리기0x04).Pointer())
	자신.M개입중단서술자표항목설정(0x04, 코드구간, 주소, 0, I개입중단서술자표개입중단관문)

	주소 = uint32(ValueOf(개입중단예외처리기0x05).Pointer())
	자신.M개입중단서술자표항목설정(0x05, 코드구간, 주소, 0, I개입중단서술자표개입중단관문)

	주소 = uint32(ValueOf(개입중단예외처리기0x06).Pointer())
	자신.M개입중단서술자표항목설정(0x06, 코드구간, 주소, 0, I개입중단서술자표개입중단관문)

	주소 = uint32(ValueOf(개입중단예외처리기0x07).Pointer())
	자신.M개입중단서술자표항목설정(0x07, 코드구간, 주소, 0, I개입중단서술자표개입중단관문)

	주소 = uint32(ValueOf(개입중단예외처리기0x08).Pointer())
	자신.M개입중단서술자표항목설정(0x08, 코드구간, 주소, 0, I개입중단서술자표개입중단관문)

	주소 = uint32(ValueOf(개입중단예외처리기0x09).Pointer())
	자신.M개입중단서술자표항목설정(0x09, 코드구간, 주소, 0, I개입중단서술자표개입중단관문)

	주소 = uint32(ValueOf(개입중단예외처리기0x0a).Pointer())
	자신.M개입중단서술자표항목설정(0x0A, 코드구간, 주소, 0, I개입중단서술자표개입중단관문)

	주소 = uint32(ValueOf(개입중단예외처리기0x0b).Pointer())
	자신.M개입중단서술자표항목설정(0x0B, 코드구간, 주소, 0, I개입중단서술자표개입중단관문)
	주소 = uint32(ValueOf(개입중단예외처리기0x0c).Pointer())
	자신.M개입중단서술자표항목설정(0x0C, 코드구간, 주소, 0, I개입중단서술자표개입중단관문)
	주소 = uint32(ValueOf(개입중단예외처리기0x0d).Pointer())
	자신.M개입중단서술자표항목설정(0x0D, 코드구간, 주소, 0, I개입중단서술자표개입중단관문)

	주소 = uint32(ValueOf(개입중단예외처리기0x0e).Pointer())
	자신.M개입중단서술자표항목설정(0x0E, 코드구간, 주소, 0, I개입중단서술자표개입중단관문)

	주소 = uint32(ValueOf(개입중단예외처리기0x0f).Pointer())
	자신.M개입중단서술자표항목설정(0x0F, 코드구간, 주소, 0, I개입중단서술자표개입중단관문)

	주소 = uint32(ValueOf(개입중단예외처리기0x10).Pointer())
	자신.M개입중단서술자표항목설정(0x10, 코드구간, 주소, 0, I개입중단서술자표개입중단관문)

	주소 = uint32(ValueOf(개입중단예외처리기0x11).Pointer())
	자신.M개입중단서술자표항목설정(0x11, 코드구간, 주소, 0, I개입중단서술자표개입중단관문)

	주소 = uint32(ValueOf(개입중단예외처리기0x12).Pointer())
	자신.M개입중단서술자표항목설정(0x12, 코드구간, 주소, 0, I개입중단서술자표개입중단관문)

	주소 = uint32(ValueOf(개입중단예외처리기0x13).Pointer())
	자신.M개입중단서술자표항목설정(0x13, 코드구간, 주소, 0, I개입중단서술자표개입중단관문)

	주소 = uint32(ValueOf(개입중단요청처리기0x00).Pointer())
	자신.M개입중단서술자표항목설정(0x20, 코드구간, 주소, 0, I개입중단서술자표개입중단관문)

	주소 = uint32(ValueOf(개입중단요청처리기0x01).Pointer())
	자신.M개입중단서술자표항목설정(0x21, 코드구간, 주소, 0, I개입중단서술자표개입중단관문)

	주소 = uint32(ValueOf(개입중단요청처리기0x02).Pointer())
	자신.M개입중단서술자표항목설정(0x22, 코드구간, 주소, 0, I개입중단서술자표개입중단관문)

	주소 = uint32(ValueOf(개입중단요청처리기0x03).Pointer())
	자신.M개입중단서술자표항목설정(0x23, 코드구간, 주소, 0, I개입중단서술자표개입중단관문)

	주소 = uint32(ValueOf(개입중단요청처리기0x04).Pointer())
	자신.M개입중단서술자표항목설정(0x24, 코드구간, 주소, 0, I개입중단서술자표개입중단관문)

	주소 = uint32(ValueOf(개입중단요청처리기0x05).Pointer())
	자신.M개입중단서술자표항목설정(0x25, 코드구간, 주소, 0, I개입중단서술자표개입중단관문)

	주소 = uint32(ValueOf(개입중단요청처리기0x06).Pointer())
	자신.M개입중단서술자표항목설정(0x26, 코드구간, 주소, 0, I개입중단서술자표개입중단관문)

	주소 = uint32(ValueOf(개입중단요청처리기0x07).Pointer())
	자신.M개입중단서술자표항목설정(0x27, 코드구간, 주소, 0, I개입중단서술자표개입중단관문)

	주소 = uint32(ValueOf(개입중단요청처리기0x08).Pointer())
	자신.M개입중단서술자표항목설정(0x28, 코드구간, 주소, 0, I개입중단서술자표개입중단관문)

	주소 = uint32(ValueOf(개입중단요청처리기0x09).Pointer())
	자신.M개입중단서술자표항목설정(0x29, 코드구간, 주소, 0, I개입중단서술자표개입중단관문)

	주소 = uint32(ValueOf(개입중단요청처리기0x0a).Pointer())
	자신.M개입중단서술자표항목설정(0x2A, 코드구간, 주소, 0, I개입중단서술자표개입중단관문)

	주소 = uint32(ValueOf(개입중단요청처리기0x0b).Pointer())
	자신.M개입중단서술자표항목설정(0x2B, 코드구간, 주소, 0, I개입중단서술자표개입중단관문)

	주소 = uint32(ValueOf(개입중단요청처리기0x0c).Pointer())
	자신.M개입중단서술자표항목설정(0x2C, 코드구간, 주소, 0, I개입중단서술자표개입중단관문)

	주소 = uint32(ValueOf(개입중단요청처리기0x0d).Pointer())
	자신.M개입중단서술자표항목설정(0x2D, 코드구간, 주소, 0, I개입중단서술자표개입중단관문)

	주소 = uint32(ValueOf(개입중단요청처리기0x0e).Pointer())
	자신.M개입중단서술자표항목설정(0x2E, 코드구간, 주소, 0, I개입중단서술자표개입중단관문)

	주소 = uint32(ValueOf(개입중단요청처리기0x0f).Pointer())
	자신.M개입중단서술자표항목설정(0x2F, 코드구간, 주소, 0, I개입중단서술자표개입중단관문)

	주소 = uint32(ValueOf(개입중단요청처리기0x80).Pointer())
	자신.M개입중단서술자표항목설정(0x80, 코드구간, 주소, 3, I개입중단서술자표개입중단관문)

	주소 = uint32(ValueOf(개입중단요청처리기0x81).Pointer())
	자신.M개입중단서술자표항목설정(0x81, 코드구간, 주소, 3, I개입중단서술자표개입중단관문)

	주소 = uint32(ValueOf(개입중단요청처리기0x82).Pointer())
	자신.M개입중단서술자표항목설정(0x82, 코드구간, 주소, 3, I개입중단서술자표개입중단관문)

	F입출력접속바이트쓰기(P주PIC명령입출력접속, 0x11)
	F입출력접속바이트쓰기(P보조PIC명령입출력접속, 0x11)

	F입출력접속바이트쓰기(P주PIC자료입출력접속, 0x20)
	F입출력접속바이트쓰기(P보조PIC자료입출력접속, 0x28)

	F입출력접속바이트쓰기(P주PIC자료입출력접속, 0x04)
	F입출력접속바이트쓰기(P보조PIC자료입출력접속, 0x02)

	F입출력접속바이트쓰기(P주PIC자료입출력접속, 0x01)
	F입출력접속바이트쓰기(P보조PIC자료입출력접속, 0x01)

	F입출력접속바이트쓰기(P주PIC자료입출력접속, 0xF8)
	F입출력접속바이트쓰기(P보조PIC자료입출력접속, 0xEF)

	개입중단서술자표주소참조 := [6]uint8{0, 0, 0, 0, 0, 0}
	크기 := (*uint16)(Pointer(&개입중단서술자표주소참조[0]))
	(*크기) = (uint16)(Sizeof(개입중단서술자표자료) - 1)

	기준 := (*uint32)(Pointer(&개입중단서술자표주소참조[2]))
	(*기준) = uint32(uintptr(Pointer(&개입중단서술자표자료)))

	F개입중단서술자표적재(uintptr(Pointer(&개입중단서술자표주소참조)))
}
func F개입중단서술자표적재(lidtaddr uintptr)

func (자신 *T개입중단관리자) M개입중단서술자표항목설정(개입중단 int,
	코드구간 uint16,
	처리기 uint32,
	D서술자privilegelevel uint8,
	D서술자종류 uint8) {

	처리기주소낮음비트들 := (*uint16)(Pointer(&개입중단서술자표자료[개입중단*8+0]))
	(*처리기주소낮음비트들) = uint16(처리기 & 0xFFFF)

	전역서술자표코드구간선택자 := (*uint16)(Pointer(&개입중단서술자표자료[개입중단*8+2]))
	(*전역서술자표코드구간선택자) = 코드구간

	예약됨 := (*uint8)(Pointer(&개입중단서술자표자료[개입중단*8+4]))
	(*예약됨) = 0

	var I개입중단서술자표서술자현재 uint8 = 0x80
	접근 := (*uint8)(Pointer(&개입중단서술자표자료[개입중단*8+5]))
	(*접근) = (I개입중단서술자표서술자현재 | D서술자종류 | ((D서술자privilegelevel & 3) << 5))

	처리기주소높음비트들 := (*uint16)(Pointer(&개입중단서술자표자료[개입중단*8+6]))
	(*처리기주소높음비트들) = uint16((처리기 >> 16) & 0xFFFF)

}

func (자신 *T개입중단관리자) M처리기설정(처리기 uintptr, T개입중단번호 uint8) {
	처리기_2[T개입중단번호] = 처리기
}
func (자신 *T개입중단관리자) M처리기가져오기(T개입중단번호 uint8) uintptr {
	return 처리기_2[T개입중단번호]
}
func (자신 *T개입중단관리자) M개입중단분배(개입중단 uint8, 확장쌓임공간주소 uint32) uint32 {

	if 개입중단추적 {
		단말_3.M지정좌표에출력("[esp:", 1, 20)
		단말_3.M부호없음정수32출력(uint32(개입중단))
		단말_3.M출력(":")
		단말_3.M부호없음정수32출력(확장쌓임공간주소)
	}
	처리기실행 := false
	if 처리기_2[개입중단] != 0 {

		myfunction := (*(*(func(uint32) uint32))(Pointer(처리기_2[개입중단])))
		확장쌓임공간주소 = myfunction(확장쌓임공간주소)
		처리기실행 = true

	}

	if !처리기실행 && 개입중단 == uint8(자신.물리장치개입중단변위) && 자신.작업관리자 != nil {
		확장쌓임공간주소 = uint32(uintptr(Pointer(자신.작업관리자.M다음작업선택((*T중앙처리장치상태)(Pointer(uintptr(확장쌓임공간주소)))))))

	}
	if !처리기실행 && 개입중단 == 0x80 {
		확장쌓임공간주소 = 미지원체계호출처리(확장쌓임공간주소)
	}

	if 개입중단 <= 0x1F {
	}
	if 0x20 <= 개입중단 && 개입중단 < 0x30 {
		if 0x28 <= 개입중단 {
			F입출력접속바이트쓰기(P보조PIC명령입출력접속, 0x20)
		}
		F입출력접속바이트쓰기(P주PIC명령입출력접속, 0x20)
	}
	return 확장쌓임공간주소
}

var 개수2 uint8 = 1

func 제어저장기3쓰기(주소 uint32)

var 단말_3 T단말 = T단말{}

func H처리개입중단(확장쌓임공간주소 uint32, 개입중단 uint32) uint32 {

	if 개입중단추적 && 개입중단 != 0x80 && 개입중단 != 0x20 {
		단말_3.M지정좌표에출력("[esp:", 1, 21)
		단말_3.M부호없음정수32출력(uint32(개입중단))
		단말_3.M출력(":")
		단말_3.M부호없음정수32출력(확장쌓임공간주소)
	}

	if A활성개입중단관리자 != 0 {
		p := (*T개입중단관리자)(Pointer(A활성개입중단관리자))
		확장쌓임공간주소 = p.M개입중단분배(uint8(개입중단), 확장쌓임공간주소)
		return 확장쌓임공간주소
	}
	if 처리기_2[개입중단] != 0 {
		myfunction := (*(*(func(uint32) uint32))(Pointer(처리기_2[개입중단])))
		확장쌓임공간주소 = myfunction(확장쌓임공간주소)
	}
	if 개입중단 == 0x80 {
		return 미지원체계호출처리(확장쌓임공간주소)
	}
	if 0x20 <= 개입중단 && 개입중단 < 0x30 {
		if 0x28 <= 개입중단 {
			F입출력접속바이트쓰기(P보조PIC명령입출력접속, 0x20)
		}
		F입출력접속바이트쓰기(P주PIC명령입출력접속, 0x20)
	}

	return 확장쌓임공간주소
}

func 미지원체계호출처리(확장쌓임공간주소 uint32) uint32 {
	중앙처리장치 := (*T중앙처리장치상태)(Pointer(uintptr(확장쌓임공간주소)))
	if 중앙처리장치.Eax == 1 || 중앙처리장치.Eax == 252 {
		중앙처리장치.Eip = uint32(ValueOf(개입중단종료대기).Pointer())
		중앙처리장치.Cs = Seg핵심코드
		중앙처리장치.Ds = Seg핵심자료
		중앙처리장치.Es = Seg핵심자료
		중앙처리장치.Fs = Seg핵심자료
		중앙처리장치.Gs = Seg핵심gs
		중앙처리장치.Ss = Seg핵심자료
		중앙처리장치.Eflags = 0x202
	}
	return 확장쌓임공간주소
}

var 제외대상 uint8 = 0

func 예외오류코드포함여부(개입중단 uint32) bool {
	switch 개입중단 {
	case 0x08, 0x0A, 0x0B, 0x0C, 0x0D, 0x0E, 0x11:
		return true
	}
	return false
}

func 예외이름(개입중단 uint32) string {
	switch 개입중단 {
	case 0x00:
		return "#DE divide error"
	case 0x06:
		return "#UD invalid opcode"
	case 0x08:
		return "#DF double fault"
	case 0x0A:
		return "#TS invalid TSS"
	case 0x0B:
		return "#NP segment not present"
	case 0x0C:
		return "#SS stack fault"
	case 0x0D:
		return "#GP general protection"
	case 0x0E:
		return "#PF page fault"
	case 0x11:
		return "#AC alignment check"
	}
	return "#EX exception"
}

func 예외저장틀값(프레임 uint32, 변위 uint32) uint32 {
	return *(*uint32)(Pointer(uintptr(프레임 + 변위)))
}

func 예외제어저장기0() uint32
func 예외제어저장기2() uint32
func 예외제어저장기3() uint32

func 기억쪽결함정보출력(오류 uint32) {
	F긴급문자열기록(" pf=[")
	if (오류 & 0x01) != 0 {
		F긴급문자열기록("protection")
	} else {
		F긴급문자열기록("not-present")
	}
	if (오류 & 0x02) != 0 {
		F긴급문자열기록(",write")
	} else {
		F긴급문자열기록(",read")
	}
	if (오류 & 0x04) != 0 {
		F긴급문자열기록(",user")
	} else {
		F긴급문자열기록(",kernel")
	}
	if (오류 & 0x08) != 0 {
		F긴급문자열기록(",reserved-bit")
	}
	if (오류 & 0x10) != 0 {
		F긴급문자열기록(",instruction-fetch")
	}
	F긴급문자열기록("]")
}

func 예외선택자정보출력(오류 uint32) {
	F긴급문자열기록(" selector=")
	F긴급정수32기록(오류 & 0xFFFFFFF8)
	F긴급문자열기록(" index=")
	F긴급정수32기록(오류 >> 3)
	F긴급문자열기록(" table=")
	if (오류 & 0x02) != 0 {
		F긴급문자열기록("IDT")
	} else if (오류 & 0x04) != 0 {
		F긴급문자열기록("LDT")
	} else {
		F긴급문자열기록("GDT")
	}
	F긴급문자열기록(" ext=")
	F긴급정수32기록(오류 & 0x01)
}

func F예외처리(확장쌓임공간주소 uint32, 개입중단 uint32) uint32 {
	F긴급문자열기록("\nEXCEPTION vec=")
	F긴급8비트16진수기록(uint8(개입중단))
	F긴급문자열기록(" ")
	F긴급문자열기록(예외이름(개입중단))
	F긴급문자열기록(" frame=")
	F긴급정수32기록(확장쌓임공간주소)

	if 확장쌓임공간주소 < 0x1000 {
		F긴급문자열기록(" invalid-frame")
		if 예외오류코드포함여부(개입중단) {
			F긴급문자열기록(" raw-error-or-bad-esp=")
			F긴급정수32기록(확장쌓임공간주소)
			예외선택자정보출력(확장쌓임공간주소)
		}
		F긴급문자열기록("\n=== ENGOS HALTED AFTER EXCEPTION ===\n")
		제외대상++
		return 확장쌓임공간주소
	}

	var 오류 uint32 = 0
	var eip변위 uint32 = 0
	if 예외오류코드포함여부(개입중단) {
		오류 = 예외저장틀값(확장쌓임공간주소, 0)
		eip변위 = 4
	}
	eip := 예외저장틀값(확장쌓임공간주소, eip변위)
	cs := 예외저장틀값(확장쌓임공간주소, eip변위+4)
	eflags := 예외저장틀값(확장쌓임공간주소, eip변위+8)

	F긴급문자열기록(" err=")
	F긴급정수32기록(오류)
	F긴급문자열기록(" eip=")
	F긴급정수32기록(eip)
	F긴급문자열기록(" cs=")
	F긴급정수32기록(cs)
	F긴급문자열기록(" eflags=")
	F긴급정수32기록(eflags)
	F긴급문자열기록(" cr0=")
	F긴급정수32기록(예외제어저장기0())
	F긴급문자열기록(" cr3=")
	F긴급정수32기록(예외제어저장기3())

	if 개입중단 == 0x0E {
		F긴급문자열기록(" cr2=")
		F긴급정수32기록(예외제어저장기2())
		기억쪽결함정보출력(오류)
	}

	if (cs & 0x03) != 0 {
		F긴급문자열기록(" useresp=")
		F긴급정수32기록(예외저장틀값(확장쌓임공간주소, eip변위+12))
		F긴급문자열기록(" ss=")
		F긴급정수32기록(예외저장틀값(확장쌓임공간주소, eip변위+16))
	}

	if 예외오류코드포함여부(개입중단) {
		예외선택자정보출력(오류)
	}
	F긴급문자열기록("\n=== ENGOS HALTED AFTER EXCEPTION ===\n")
	제외대상++

	return 확장쌓임공간주소
}

func 치명적예외후중지()

func F치명적개입중단저장틀처리(저장됨확장쌓임공간주소 uint32, 개입중단 uint32) uint32 {
	F예외처리(저장됨확장쌓임공간주소+52, 개입중단)
	치명적예외후중지()
	return 저장됨확장쌓임공간주소
}

func F개입중단허용()
func (자신 *T개입중단관리자) M개입중단활성화() {
	if A활성개입중단관리자 != 0 {
		자신.M개입중단비활성화()
	}
	주소 := uintptr(Pointer(자신))
	A활성개입중단관리자 = 주소
	F개입중단허용()
}
func F개입중단금지()
func (자신 *T개입중단관리자) M개입중단비활성화() {
	A활성개입중단관리자 = 0
	F개입중단금지()
}

func F시험용개입중단처리(개입중단 uint8, 확장쌓임공간주소 uint32) uint32 {
	임시공간 := []byte("interrupt")
	단말_3 := T단말{}
	단말_3.M출력(임시공간)
	return 확장쌓임공간주소
}
func F개입중단시험(개입중단 uint8, 확장쌓임공간주소 uint32)

func F미처리개입중단알림() {
	임시공간 := []byte("unhandle interrupt\n")
	단말_3 := T단말{}
	단말_3.M출력(임시공간)
}

func 개입중단처리기_2(개입중단 uint8, 확장쌓임공간주소 uint32) uint32 {
	임시공간 := []byte("interrupt\nhandler\nhi")
	단말_3 := T단말{}
	단말_3.M출력(임시공간)
	단말_3.M16진수출력(0x40)
	return 확장쌓임공간주소
}
func 쌓임공간주소출력(확장쌓임공간주소 uint32) {
	단말_3 := T단말{}
	단말_3.M지정좌표에정수32출력(확장쌓임공간주소, 20, 21)
}
func 실행흐름지역주소읽기() uint32
func F실행흐름지역주소출력() {

}
