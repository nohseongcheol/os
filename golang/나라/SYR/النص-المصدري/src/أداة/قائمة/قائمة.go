package قائمة

import . "unsafe"
import . "طرفية"
import mem "ذاكرةمدير"

type Tعقدة_قائمة struct {
	مرجع_عنوان		uintptr
	previous	*Tعقدة_قائمة
	التالي		*Tعقدة_قائمة
}

type Linkedقائمة struct {
	head		*Tعقدة_قائمة
	tail		*Tعقدة_قائمة
	Sالحجم_2	int

	mem	*mem.Tذاكرةمدير
}

func (نفسه *Linkedقائمة) Init(mem *mem.Tذاكرةمدير) {
	نفسه.head = nil
	نفسه.tail = nil
	نفسه.Sالحجم_2 = 0

	نفسه.mem = mem
}
func (نفسه *Linkedقائمة) Mإضافة_إلى_بداية_القائمة(مرجع_عنوان uintptr) {
	جديدالعقدة := (*Tعقدة_قائمة)(نفسه.mem.Mتخصيص_الذاكرة(uint32(Sizeof(Tعقدة_قائمة{}))))
	if جديدالعقدة == nil {
		return
	}
	جديدالعقدة.مرجع_عنوان = مرجع_عنوان
	جديدالعقدة.previous = nil
	جديدالعقدة.التالي = نفسه.head
	if نفسه.head != nil {
		نفسه.head.previous = جديدالعقدة
	}
	نفسه.head = جديدالعقدة
	نفسه.Sالحجم_2++

	if نفسه.head.التالي == nil {
		نفسه.tail = نفسه.head
	}

}
func (نفسه *Linkedقائمة) Mإضافة_إلى_نهاية_القائمة(مرجع_عنوان uintptr) {
	if نفسه.Sالحجم_2 == 0 {
		نفسه.Mإضافة_إلى_بداية_القائمة(مرجع_عنوان)
	} else {
		جديدالعقدة := (*Tعقدة_قائمة)(نفسه.mem.Mتخصيص_الذاكرة(uint32(Sizeof(Tعقدة_قائمة{}))))
		if جديدالعقدة == nil {
			return
		}
		جديدالعقدة.مرجع_عنوان = مرجع_عنوان
		جديدالعقدة.previous = نفسه.tail
		جديدالعقدة.التالي = nil
		نفسه.tail.التالي = جديدالعقدة
		نفسه.tail = جديدالعقدة
		نفسه.Sالحجم_2++
	}
}
func (نفسه *Linkedقائمة) Mإدراج_عند_الفهرس(فهرس int, مرجع_عنوان uintptr) {
	if فهرس == 0 {
		نفسه.Mإضافة_إلى_بداية_القائمة(مرجع_عنوان)
	} else {
		previousالعقدة := نفسه.Getالعقدةat(فهرس - 1)
		التاليالعقدة := previousالعقدة.التالي
		جديدالعقدة := (*Tعقدة_قائمة)(نفسه.mem.Mتخصيص_الذاكرة(uint32(Sizeof(Tعقدة_قائمة{}))))
		if جديدالعقدة == nil {
			return
		}
		جديدالعقدة.مرجع_عنوان = مرجع_عنوان

		previousالعقدة.التالي = جديدالعقدة
		جديدالعقدة.previous = previousالعقدة
		جديدالعقدة.التالي = التاليالعقدة
		if التاليالعقدة != nil {
			التاليالعقدة.previous = جديدالعقدة
		}

		نفسه.Sالحجم_2++

		if جديدالعقدة.التالي == nil {
			نفسه.tail = جديدالعقدة
		}
	}
}
func (نفسه *Linkedقائمة) Getالعقدةat(فهرس int) *Tعقدة_قائمة {
	if فهرس < 0 || فهرس >= نفسه.Sالحجم_2 {
		return nil
	}
	var x *Tعقدة_قائمة = نفسه.head
	for i := 0; i < فهرس; i++ {
		x = x.التالي
	}
	return x
}

func (نفسه *Linkedقائمة) Sتحديدالعقدةat(فهرس int, مرجع_عنوان uintptr) {
	var x *Tعقدة_قائمة = نفسه.head
	for i := 0; i < فهرس; i++ {
		x = x.التالي
	}
	if x != nil {
		x.مرجع_عنوان = مرجع_عنوان
	}
}
func (نفسه *Linkedقائمة) Getat(فهرس int) Pointer {
	عقدة_قائمة := نفسه.Getالعقدةat(فهرس)
	if عقدة_قائمة == nil {
		return nil
	}
	var مرجع_عنوان uintptr = عقدة_قائمة.مرجع_عنوان
	return Pointer(مرجع_عنوان)
}
func (نفسه *Linkedقائمة) Iفهرسof(مرجع_عنوان uintptr) int {
	var n *Tعقدة_قائمة = نفسه.head
	i := 0
	for ; i < نفسه.Sالحجم_2; i++ {
		if مرجع_عنوان == n.مرجع_عنوان {
			return i
		}
		n = n.التالي
	}
	return -1
}
func (نفسه *Linkedقائمة) Rأزل(مرجع_عنوان uintptr) {
	فهرس := نفسه.Iفهرسof(مرجع_عنوان)
	if فهرس < 0 {
		return
	}
	نفسه.Rأزلat(فهرس)
}
func (نفسه *Linkedقائمة) Rأزلat(فهرس int) {
	if فهرس < 0 || فهرس >= نفسه.Sالحجم_2 {
		return
	}
	عقدة_قائمة := نفسه.Getالعقدةat(فهرس)
	if عقدة_قائمة == nil {
		return
	}
	if عقدة_قائمة.previous != nil {
		عقدة_قائمة.previous.التالي = عقدة_قائمة.التالي
	} else {
		نفسه.head = عقدة_قائمة.التالي
	}
	if عقدة_قائمة.التالي != nil {
		عقدة_قائمة.التالي.previous = عقدة_قائمة.previous
	} else {
		نفسه.tail = عقدة_قائمة.previous
	}
	نفسه.Sالحجم_2 = نفسه.Sالحجم_2 - 1

	if نفسه.mem != nil {
		نفسه.mem.Fخالي(Pointer(عقدة_قائمة))
	}
}

var طرفية_2 = Tطرفية{}

func (نفسه *Linkedقائمة) Pاطبع() {
	طرفية_2.Mاطبعxy("LinkedList:", 1, 1)
	طرفية_2.MUnsignedinteger32اطبع(uint32(uintptr(Pointer(نفسه))))
	for i := 0; i < نفسه.Sالحجم_2; i++ {
		عقدة_قائمة := (*Tعقدة_قائمة)(نفسه.Getat(i))
		طرفية_2.MUnsignedinteger32اطبع(uint32(عقدة_قائمة.مرجع_عنوان))
		طرفية_2.Mاطبع(":")
	}
}
