/*
	Copyright 2020. (노성철, nsch78@nate.com, nsch@naver.com) All right reserved
*/

#include <தொடர்பு_முகவரி_மாற்றம்/எட்டு_இரும_இலக்கக்_குழு_வரிசை.h>
#include <errno.h>
#include <fcntl.h>
#include <பிணையங்களிடை/முகவரி.h>
#include <அடிப்படை_வரையறைகள்.h>
#include <அமைப்பு/socket.h>
#include <அமைப்பு/stat.h>
#include <அமைப்பு/அமைப்பு_அடையாளம்.h>
#include <அமைப்பு/சேய்_காத்திருப்பு.h>
#include <unistd.h>
#include "shell_locale.h"

/* Adapted from koros4's request interpreter. Private names are localized
 * in each WorldOS edition; main and POSIX ABI names remain unchanged. */

enum { உள்ளீட்டு_வரியின்_கொள்ளளவு = 512, செயலுருபுகளின்_உச்ச_எண்ணிக்கை = 16, கட்டளைக்_கோப்புகளின்_உச்ச_அடுக்கு = 4 };
static int கட்டளைக்_கோப்புகளின்_அடுக்கு_ஆழம்;
struct உள்ளீட்டு_ஓடை {
    int உள்ளீட்டு_விவரிப்பி;
    char பரிமாற்ற_இடையகம்_2[256];
    size_t இடம்;
    size_t நீளம்;
};

static size_t உரையின்_எண்மி_நீளம்(const char *உரை)
{
    size_t நீளம் = 0;
    while (உரை[நீளம்] != '\0')
        நீளம்++;
    return நீளம்;
}

static int உரைகள்_சமம்(const char *இடது, const char *வலது)
{
    size_t இடம் = 0;
    while (இடது[இடம்] == வலது[இடம்]) {
        if (இடது[இடம்] == '\0')
            return 1;
        இடம்++;
    }
    return 0;
}

static void உரையை_எழுது(const char *உரை)
{
    size_t நீளம் = உரையின்_எண்மி_நீளம்(உரை);
    while (நீளம் > 0U) {
        ssize_t எழுதிய_எண்மிகளின்_எண்ணிக்கை = எழுது(STDOUT_FILENO, உரை, நீளம்);
        if (எழுதிய_எண்மிகளின்_எண்ணிக்கை <= 0)
            return;
        உரை += எழுதிய_எண்மிகளின்_எண்ணிக்கை;
        நீளம் -= (size_t)எழுதிய_எண்மிகளின்_எண்ணிக்கை;
    }
}

static void முழு_எண்ணை_எழுது(int மதிப்பு)
{
    char இலக்க_எழுத்துகள்[16];
    unsigned int இலக்கங்களின்_எண்ணிக்கை;
    unsigned int குறியற்ற_அளவு;

    if (மதிப்பு < 0) {
        உரையை_எழுது("-");
        குறியற்ற_அளவு = (unsigned int)(-(மதிப்பு + 1)) + 1U;
    } else {
        குறியற்ற_அளவு = (unsigned int)மதிப்பு;
    }
    இலக்கங்களின்_எண்ணிக்கை = 0;
    do {
        இலக்க_எழுத்துகள்[இலக்கங்களின்_எண்ணிக்கை++] = (char)('0' + குறியற்ற_அளவு % 10U);
        குறியற்ற_அளவு /= 10U;
    } while (குறியற்ற_அளவு != 0U);
    while (இலக்கங்களின்_எண்ணிக்கை > 0U) {
        இலக்கங்களின்_எண்ணிக்கை--;
        (void)எழுது(STDOUT_FILENO, &இலக்க_எழுத்துகள்[இலக்கங்களின்_எண்ணிக்கை], 1);
    }
}

static void பிழையை_அறிவி(const char *செயல்)
{
    உரையை_எழுது("error: ");
    உரையை_எழுது(செயல்);
    உரையை_எழுது(" errno=");
    முழு_எண்ணை_எழுது(errno);
    உரையை_எழுது("\n");
}

static int உள்ளீட்டு_வரியைப்_படி(struct உள்ளீட்டு_ஓடை *உள்ளீடு, char *உள்ளீட்டு_வரி, size_t கொள்ளளவு)
{
    size_t இடம் = 0;
    int செல்லாத_உள்ளீட்டு_வரி = 0;
    char எழுத்து;
    ssize_t படித்த_எண்மிகள்;
    if (கொள்ளளவு < 2U)
        return -2;
    for (;;) {
        if (உள்ளீடு->இடம் == உள்ளீடு->நீளம்) {
            படித்த_எண்மிகள் = படி(உள்ளீடு->உள்ளீட்டு_விவரிப்பி, உள்ளீடு->பரிமாற்ற_இடையகம்_2, sizeof(உள்ளீடு->பரிமாற்ற_இடையகம்_2));
            if (படித்த_எண்மிகள் < 0) {
                if (errno == EINTR)
                    continue;
                return -1;
            }
            if (படித்த_எண்மிகள் == 0) {
                if (இடம் == 0 && !செல்லாத_உள்ளீட்டு_வரி)
                    return -1;
                break;
            }
            உள்ளீடு->நீளம் = (size_t)படித்த_எண்மிகள்;
            உள்ளீடு->இடம் = 0;
        }
        எழுத்து = உள்ளீடு->பரிமாற்ற_இடையகம்_2[உள்ளீடு->இடம்++];
        if (எழுத்து == '\n')
            break;
        if (உள்ளீடு->உள்ளீட்டு_விவரிப்பி == STDIN_FILENO && எழுத்து == 4) {
            if (இடம் == 0 && !செல்லாத_உள்ளீட்டு_வரி)
                return -1;
            break;
        }
        if (உள்ளீடு->உள்ளீட்டு_விவரிப்பி == STDIN_FILENO && (எழுத்து == 8 || எழுத்து == 127)) {
            if (இடம் > 0) {
                do {
                    இடம்--;
                } while (இடம் > 0 && ((unsigned char)உள்ளீட்டு_வரி[இடம்] & 0xc0U) == 0x80U);
            }
            continue;
        }
        if (எழுத்து == '\r')
            continue;
        if (எழுத்து == '\0') {
            செல்லாத_உள்ளீட்டு_வரி = 1; /* Reject binary input; do not execute its prefix. */
            continue;
        }
        if (இடம் + 1U < கொள்ளளவு)
            உள்ளீட்டு_வரி[இடம்++] = எழுத்து;
        else
            செல்லாத_உள்ளீட்டு_வரி = 1;
    }
    உள்ளீட்டு_வரி[இடம்] = '\0';
    return செல்லாத_உள்ளீட்டு_வரி ? -2 : (int)இடம்;
}

static int செயலுருபுகளைப்_பிரி(char *உள்ளீட்டு_வரி, char **செயலுருபுகள்)
{
    int செயலுருபுகளின்_எண்ணிக்கை = 0;
    char *நடப்பு_இடம் = உள்ளீட்டு_வரி;
    char *வெளியீட்டு_இடம் = உள்ளீட்டு_வரி;

    while (*நடப்பு_இடம் != '\0') {
        char மேற்கோள்_குறி = '\0';
        while (*நடப்பு_இடம் == ' ' || *நடப்பு_இடம் == '\t')
            நடப்பு_இடம்++;
        if (*நடப்பு_இடம் == '\0' || *நடப்பு_இடம் == '#')
            break;
        if (செயலுருபுகளின்_எண்ணிக்கை == செயலுருபுகளின்_உச்ச_எண்ணிக்கை - 1)
            return -1;
        செயலுருபுகள்[செயலுருபுகளின்_எண்ணிக்கை++] = வெளியீட்டு_இடம்;
        while (*நடப்பு_இடம் != '\0') {
            char எழுத்து = *நடப்பு_இடம்++;
            if (மேற்கோள்_குறி == '\0' && (எழுத்து == ' ' || எழுத்து == '\t'))
                break;
            if (எழுத்து == '\\' && மேற்கோள்_குறி != '\'') {
                if (*நடப்பு_இடம் == '\0')
                    return -1;
                *வெளியீட்டு_இடம்++ = *நடப்பு_இடம்++;
            } else if (எழுத்து == '\'' || எழுத்து == '"') {
                if (மேற்கோள்_குறி == '\0')
                    மேற்கோள்_குறி = எழுத்து;
                else if (மேற்கோள்_குறி == எழுத்து)
                    மேற்கோள்_குறி = '\0';
                else
                    *வெளியீட்டு_இடம்++ = எழுத்து;
            } else {
                *வெளியீட்டு_இடம்++ = எழுத்து;
            }
        }
        if (மேற்கோள்_குறி != '\0')
            return -1;
        *வெளியீட்டு_இடம்++ = '\0';
    }
    செயலுருபுகள்[செயலுருபுகளின்_எண்ணிக்கை] = (char *)0;
    return செயலுருபுகளின்_எண்ணிக்கை;
}

static void உதவியைக்_காட்டு(void)
{
    size_t இடம்;
    உரையை_எழுது(
        "WorldOS command interpreter commands:\n"
        "  help                 show this help\n"
        "  echo TEXT            print text\n"
        "  pwd                  show current directory\n"
        "  cd PATH              change directory\n"
        "  cat FILE             print a FAT file\n"
        "  stat FILE            show file size\n"
        "  pid                  show process identifiers\n"
        "  uname                show operating-system identity\n"
        "  run FILE [ARGS...]   execute a user ELF program\n"
        "  udp [TEXT]           call POSIX UDP and loop back TEXT\n"
        "  source FILE          interpret a UTF-8 command file\n"
        "  exit                 leave the shell\n");
    உரையை_எழுது("Native command proposals (ASCII aliases remain available):\n");
    for (இடம் = 0; இடம் < sizeof(அடிப்படைக்_கட்டளைகள்) / sizeof(அடிப்படைக்_கட்டளைகள்[0]); இடம்++) {
        உரையை_எழுது(உள்ளூர்_கட்டளைப்_பெயர்கள்[இடம்]);
        உரையை_எழுது(" = ");
        உரையை_எழுது(அடிப்படைக்_கட்டளைகள்[இடம்]);
        உரையை_எழுது("\n");
    }
}

static int கட்டளை_பொருந்துகிறது(const char *உரை, const char *கட்டளை)
{
    size_t இடம்;
    if (உரைகள்_சமம்(உரை, கட்டளை))
        return 1;
    for (இடம் = 0; இடம் < sizeof(அடிப்படைக்_கட்டளைகள்) / sizeof(அடிப்படைக்_கட்டளைகள்[0]); இடம்++)
        if (உரைகள்_சமம்(கட்டளை, அடிப்படைக்_கட்டளைகள்[இடம்]))
            return உரைகள்_சமம்(உரை, உள்ளூர்_கட்டளைப்_பெயர்கள்[இடம்]);
    return 0;
}

static int உள்ளீட்டை_விளக்கு(int உள்ளீட்டு_விவரிப்பி);

static int கட்டளைக்_கோப்பை_விளக்கு(const char *கோப்பின்_பெயர்)
{
    int கோப்பு_விவரிப்பி_2;
    int நிலை;
    if (கட்டளைக்_கோப்புகளின்_அடுக்கு_ஆழம் >= கட்டளைக்_கோப்புகளின்_உச்ச_அடுக்கு) {
        உரையை_எழுது("source: nesting limit\n");
        return 0;
    }
    கோப்பு_விவரிப்பி_2 = திற(கோப்பின்_பெயர், O_RDONLY);
    if (கோப்பு_விவரிப்பி_2 < 0) {
        பிழையை_அறிவி(கோப்பின்_பெயர்);
        return 0;
    }
    கட்டளைக்_கோப்புகளின்_அடுக்கு_ஆழம்++;
    நிலை = உள்ளீட்டை_விளக்கு(கோப்பு_விவரிப்பி_2);
    கட்டளைக்_கோப்புகளின்_அடுக்கு_ஆழம்--;
    (void)மூடு(கோப்பு_விவரிப்பி_2);
    return நிலை;
}

static void செயலுருபுகளைக்_காட்டு(int செயலுருபுகளின்_எண்ணிக்கை, char **செயலுருபுகள்)
{
    int இடம்;
    for (இடம் = 1; இடம் < செயலுருபுகளின்_எண்ணிக்கை; இடம்++) {
        if (இடம் != 1)
            உரையை_எழுது(" ");
        உரையை_எழுது(செயலுருபுகள்[இடம்]);
    }
    உரையை_எழுது("\n");
}

static void நடப்பு_அடைவைக்_காட்டு(void)
{
    char பாதை[128];
    if (பணி_அடைவின்_பாதையைப்_பெறு(பாதை, sizeof(பாதை)) == (char *)0) {
        பிழையை_அறிவி("pwd");
        return;
    }
    உரையை_எழுது(பாதை);
    உரையை_எழுது("\n");
}

static void கோப்பின்_உள்ளடக்கத்தைக்_காட்டு(const char *கோப்பின்_பெயர்)
{
    char பரிமாற்ற_இடையகம்_2[128];
    int கோப்பு_விவரிப்பி_2 = திற(கோப்பின்_பெயர், O_RDONLY);
    ssize_t படித்த_எண்மிகள்;

    if (கோப்பு_விவரிப்பி_2 < 0) {
        பிழையை_அறிவி("cat");
        return;
    }
    while ((படித்த_எண்மிகள் = படி(கோப்பு_விவரிப்பி_2, பரிமாற்ற_இடையகம்_2, sizeof(பரிமாற்ற_இடையகம்_2))) > 0)
        (void)எழுது(STDOUT_FILENO, பரிமாற்ற_இடையகம்_2, (size_t)படித்த_எண்மிகள்);
    if (படித்த_எண்மிகள் < 0)
        பிழையை_அறிவி("cat/read");
    (void)மூடு(கோப்பு_விவரிப்பி_2);
    உரையை_எழுது("\n");
}

static void கோப்பின்_தகவலைக்_காட்டு(const char *கோப்பின்_பெயர்)
{
    struct கோப்பு_நிலை நிலை;
    if (கோப்பு_நிலை(கோப்பின்_பெயர், &நிலை) < 0) {
        பிழையை_அறிவி("stat");
        return;
    }
    உரையை_எழுது("size=");
    முழு_எண்ணை_எழுது((int)நிலை.st_size);
    உரையை_எழுது(S_ISDIR(நிலை.st_mode) ? " type=directory\n" : " type=file\n");
}

static void செயல்முறை_அடையாளங்களைக்_காட்டு(void)
{
    உரையை_எழுது("pid=");
    முழு_எண்ணை_எழுது((int)செயல்முறை_அடையாளத்தைப்_பெறு());
    உரையை_எழுது(" ppid=");
    முழு_எண்ணை_எழுது((int)தாய்_செயல்முறை_அடையாளத்தைப்_பெறு());
    உரையை_எழுது("\n");
}

static void அமைப்பின்_அடையாளத்தைக்_காட்டு(void)
{
    struct utsname அமைப்பின்_அடையாளம்;
    if (அமைப்புத்_தகவலைப்_பெறு(&அமைப்பின்_அடையாளம்) < 0) {
        பிழையை_அறிவி("uname");
        return;
    }
    உரையை_எழுது(அமைப்பின்_அடையாளம்.sysname);
    உரையை_எழுது(" ");
    உரையை_எழுது(அமைப்பின்_அடையாளம்.release);
    உரையை_எழுது(" ");
    உரையை_எழுது(அமைப்பின்_அடையாளம்.machine);
    உரையை_எழுது("\n");
}

static void நிரலை_இயக்கு(int செயலுருபுகளின்_எண்ணிக்கை, char **செயலுருபுகள்)
{
    pid_t சேய்_செயல்முறையின்_அடையாளம்;
    int சேய்_செயல்முறையின்_முடிவு_நிலை = 0;

    if (செயலுருபுகளின்_எண்ணிக்கை < 2) {
        உரையை_எழுது("usage: run FILE [ARGS...]\n");
        return;
    }
    சேய்_செயல்முறையின்_அடையாளம் = சேய்_செயல்முறையை_உருவாக்கு();
    if (சேய்_செயல்முறையின்_அடையாளம் < 0) {
        பிழையை_அறிவி("fork");
        return;
    }
    if (சேய்_செயல்முறையின்_அடையாளம் == 0) {
        இயங்கும்_நிரலை_மாற்று(செயலுருபுகள்[1], &செயலுருபுகள்[1], (char *const *)0);
        பிழையை_அறிவி("execve");
        உடனே_முடி(127);
    }
    if (குறித்த_சேய்க்குக்_காத்திரு(சேய்_செயல்முறையின்_அடையாளம், &சேய்_செயல்முறையின்_முடிவு_நிலை, 0) < 0) {
        பிழையை_அறிவி("waitpid");
        return;
    }
    உரையை_எழுது("exit-status=");
    முழு_எண்ணை_எழுது(WEXITSTATUS(சேய்_செயல்முறையின்_முடிவு_நிலை));
    உரையை_எழுது("\n");
}

static void தனிச்_செய்தியின்_மீள்வரவைச்_சோதி(const char *செய்தி)
{
    struct பிணையங்களிடை_முனை_முகவரி பெறுநரின்_முகவரி = {0};
    struct பிணையங்களிடை_முனை_முகவரி அனுப்புநரின்_முகவரி = {0};
    முகவரி_நீள_வகை அனுப்புநரின்_முகவரி_நீளம் = sizeof(அனுப்புநரின்_முகவரி);
    char பெற்ற_தரவு[96];
    size_t செய்தியின்_எண்மி_நீளம் = உரையின்_எண்மி_நீளம்(செய்தி);
    int பெறும்_தொடர்பு_முனை = -1;
    int அனுப்பும்_தொடர்பு_முனை = -1;
    ssize_t பெற்ற_எண்மிகளின்_எண்ணிக்கை;

    if (செய்தியின்_எண்மி_நீளம் >= sizeof(பெற்ற_தரவு)) {
        உரையை_எழுது("udp: message exceeds 95 bytes\n");
        return;
    }
    பெறும்_தொடர்பு_முனை = தொடர்பு_முனையை_உருவாக்கு(பிணையங்களிடை_முகவரிக்_குடும்பக்_குறியீடு, தரவுச்_செய்தி_முனை, பயனர்_தரவுச்_செய்தி_நெறிமுறை);
    அனுப்பும்_தொடர்பு_முனை = தொடர்பு_முனையை_உருவாக்கு(பிணையங்களிடை_முகவரிக்_குடும்பக்_குறியீடு, தரவுச்_செய்தி_முனை, பயனர்_தரவுச்_செய்தி_நெறிமுறை);
    if (பெறும்_தொடர்பு_முனை < 0 || அனுப்பும்_தொடர்பு_முனை < 0) {
        பிழையை_அறிவி("socket");
        goto தொடர்பு_முனைகளை_மூடு;
    }
    பெறுநரின்_முகவரி.பிணையங்களிடை_முகவரிக்_குடும்பம் = பிணையங்களிடை_முகவரிக்_குடும்பக்_குறியீடு;
    பெறுநரின்_முகவரி.தொடர்பு_வாயில்_எண் = _16_இரும_இலக்கங்களை_வலை_வரிசைக்கு_மாற்று(40404);
    பெறுநரின்_முகவரி.பிணையங்களிடை_முகவரி_உள்ளடக்கம்.முகவரி_மதிப்பு = _32_இரும_இலக்கங்களை_வலை_வரிசைக்கு_மாற்று(தன்னிடமே_திரும்பும்_முகவரி);
    if (உள்ளக_முகவரியைப்_பிணை(பெறும்_தொடர்பு_முனை, (const struct தொடர்பு_முனை_முகவரி *)&பெறுநரின்_முகவரி, sizeof(பெறுநரின்_முகவரி)) < 0) {
        பிழையை_அறிவி("bind");
        goto தொடர்பு_முனைகளை_மூடு;
    }
    if (எதிர்_முனையுடன்_இணை(அனுப்பும்_தொடர்பு_முனை, (const struct தொடர்பு_முனை_முகவரி *)&பெறுநரின்_முகவரி, sizeof(பெறுநரின்_முகவரி)) < 0) {
        பிழையை_அறிவி("connect");
        goto தொடர்பு_முனைகளை_மூடு;
    }
    if (அனுப்பு(அனுப்பும்_தொடர்பு_முனை, செய்தி, செய்தியின்_எண்மி_நீளம், 0) != (ssize_t)செய்தியின்_எண்மி_நீளம்) {
        பிழையை_அறிவி("send");
        goto தொடர்பு_முனைகளை_மூடு;
    }
    பெற்ற_எண்மிகளின்_எண்ணிக்கை = அனுப்பிய_முகவரியுடன்_பெறு(பெறும்_தொடர்பு_முனை, பெற்ற_தரவு, sizeof(பெற்ற_தரவு) - 1U, 0,
                         (struct தொடர்பு_முனை_முகவரி *)&அனுப்புநரின்_முகவரி, &அனுப்புநரின்_முகவரி_நீளம்);
    if (பெற்ற_எண்மிகளின்_எண்ணிக்கை < 0) {
        பிழையை_அறிவி("recvfrom");
        goto தொடர்பு_முனைகளை_மூடு;
    }
    பெற்ற_தரவு[பெற்ற_எண்மிகளின்_எண்ணிக்கை] = '\0';
    உரையை_எழுது("udp-received: ");
    உரையை_எழுது(பெற்ற_தரவு);
    உரையை_எழுது("\n");

தொடர்பு_முனைகளை_மூடு:
    if (அனுப்பும்_தொடர்பு_முனை >= 0)
        (void)மூடு(அனுப்பும்_தொடர்பு_முனை);
    if (பெறும்_தொடர்பு_முனை >= 0)
        (void)மூடு(பெறும்_தொடர்பு_முனை);
}

static int உள்ளீட்டை_விளக்கு(int உள்ளீட்டு_விவரிப்பி)
{
    char உள்ளீட்டு_வரி[உள்ளீட்டு_வரியின்_கொள்ளளவு];
    char *செயலுருபுகள்[செயலுருபுகளின்_உச்ச_எண்ணிக்கை];
    struct உள்ளீட்டு_ஓடை உள்ளீடு = {0};
    உள்ளீடு.உள்ளீட்டு_விவரிப்பி = உள்ளீட்டு_விவரிப்பி;

    for (;;) {
        int செயலுருபுகளின்_எண்ணிக்கை;
        int நிலை;
        if (உள்ளீட்டு_விவரிப்பி == STDIN_FILENO)
            உரையை_எழுது("worldos$ ");
        நிலை = உள்ளீட்டு_வரியைப்_படி(&உள்ளீடு, உள்ளீட்டு_வரி, sizeof(உள்ளீட்டு_வரி));
        if (நிலை == -1)
            return 0;
        if (நிலை == -2) {
            உரையை_எழுது("input rejected: overlong or binary line\n");
            continue;
        }
        செயலுருபுகளின்_எண்ணிக்கை = செயலுருபுகளைப்_பிரி(உள்ளீட்டு_வரி, செயலுருபுகள்);
        if (செயலுருபுகளின்_எண்ணிக்கை < 0) {
            உரையை_எழுது("syntax error: quote, escape or argument limit\n");
            continue;
        }
        if (செயலுருபுகளின்_எண்ணிக்கை == 0)
            continue;
        if (கட்டளை_பொருந்துகிறது(செயலுருபுகள்[0], "help"))
            உதவியைக்_காட்டு();
        else if (கட்டளை_பொருந்துகிறது(செயலுருபுகள்[0], "echo"))
            செயலுருபுகளைக்_காட்டு(செயலுருபுகளின்_எண்ணிக்கை, செயலுருபுகள்);
        else if (கட்டளை_பொருந்துகிறது(செயலுருபுகள்[0], "pwd"))
            நடப்பு_அடைவைக்_காட்டு();
        else if (கட்டளை_பொருந்துகிறது(செயலுருபுகள்[0], "cd")) {
            if (செயலுருபுகளின்_எண்ணிக்கை < 2)
                உரையை_எழுது("usage: cd PATH\n");
            else if (பணி_அடைவை_மாற்று(செயலுருபுகள்[1]) < 0)
                பிழையை_அறிவி("cd");
        } else if (கட்டளை_பொருந்துகிறது(செயலுருபுகள்[0], "cat")) {
            if (செயலுருபுகளின்_எண்ணிக்கை < 2)
                உரையை_எழுது("usage: cat FILE\n");
            else
                கோப்பின்_உள்ளடக்கத்தைக்_காட்டு(செயலுருபுகள்[1]);
        } else if (கட்டளை_பொருந்துகிறது(செயலுருபுகள்[0], "stat")) {
            if (செயலுருபுகளின்_எண்ணிக்கை < 2)
                உரையை_எழுது("usage: stat FILE\n");
            else
                கோப்பின்_தகவலைக்_காட்டு(செயலுருபுகள்[1]);
        } else if (கட்டளை_பொருந்துகிறது(செயலுருபுகள்[0], "pid"))
            செயல்முறை_அடையாளங்களைக்_காட்டு();
        else if (கட்டளை_பொருந்துகிறது(செயலுருபுகள்[0], "uname"))
            அமைப்பின்_அடையாளத்தைக்_காட்டு();
        else if (கட்டளை_பொருந்துகிறது(செயலுருபுகள்[0], "run"))
            நிரலை_இயக்கு(செயலுருபுகளின்_எண்ணிக்கை, செயலுருபுகள்);
        else if (கட்டளை_பொருந்துகிறது(செயலுருபுகள்[0], "udp"))
            தனிச்_செய்தியின்_மீள்வரவைச்_சோதி(செயலுருபுகளின்_எண்ணிக்கை >= 2 ? செயலுருபுகள்[1] : "ping");
        else if (கட்டளை_பொருந்துகிறது(செயலுருபுகள்[0], "source")) {
            if (செயலுருபுகளின்_எண்ணிக்கை < 2)
                உரையை_எழுது("usage: source FILE\n");
            else if (கட்டளைக்_கோப்பை_விளக்கு(செயலுருபுகள்[1]))
                return 1;
        } else if (கட்டளை_பொருந்துகிறது(செயலுருபுகள்[0], "exit"))
            return 1;
        else
            உரையை_எழுது("unknown command; type help\n");
    }
}

int main(void)
{
    உரையை_எழுது("WORLDOS-SHELL:READY\n");
    (void)உள்ளீட்டை_விளக்கு(STDIN_FILENO);
    உரையை_எழுது("WORLDOS-SHELL:EXIT\n");
    return 0;
}
